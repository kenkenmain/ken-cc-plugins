# Phase 4.2: Final Review [PHASE 4.2]

## Subagent Config

- **Type:** codex-reviewer (uses codex-xhigh MCP)
- **Input:** all phase outputs
- **Output:** `.agents/tmp/phases/4.2-final-review.json`

## Instructions

Perform final holistic review using highest reasoning tier.

### Codex Dispatch

```
mcp__codex-xhigh__codex(
  prompt: "Final review of all changes. Plan: .agents/tmp/phases/1.2-plan.md. Tasks: .agents/tmp/phases/2.1-tasks.json. Tests: .agents/tmp/phases/3.1-test-results.json. Criteria: prompts/high-stakes/final-review.md. Return JSON with status, overallQuality, issues[], metrics, summary, readyForCommit.",
  cwd: "{project dir}"
)
```

### Output

Write Codex response as JSON to `.agents/tmp/phases/4.2-final-review.json`

### Decision

- `approved` + `readyForCommit: true` -> proceed to completion
- `blocked` -> halt workflow, report to user
